import React, { useEffect, useState } from 'react';

import {
  StyleSheet,
  Text,
  View,
} from 'react-native';

const FavouritesScreen = ({navigation, route}) => { 
    return (
        <View  style={{backgroundColor: '#ebfaf3',flex:1}}>
            <Text>Favourites</Text>
        </View>

    );
}
export default FavouritesScreen;