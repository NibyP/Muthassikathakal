import { DefaultTheme } from 'react-native-paper';

export default theme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: '#ad0bfd',
    accent: '#f1c40f',
    background: '#ad0bfd',
    text: 'white',
  },
};